Here is your goal — I went ahead and wrote it since the task seemed clear.

```text
/goal Replace the authentication middleware with the new provider.

Scope: src/middleware/auth.*; excludes the login UI.

Complete only when all applicable conditions are true:
1. The middleware is replaced.
2. `npm test -- auth` passes.
3. A manual login round trip succeeds.

Stop only if the provider credentials are unavailable.
```

ready
